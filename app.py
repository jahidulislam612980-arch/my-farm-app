import streamlit as st
import gspread
from oauth2client.service_account import ServiceAccountCredentials
import pandas as pd
from datetime import datetime

# ১. গুগল শিট কানেকশন সেটআপ
scope = ["https://spreadsheets.google.com/feeds", "https://www.googleapis.com/auth/drive"]

# এখানে আপনার সেই দীর্ঘ JSON ফাইলটি (Credentials) বসাতে হবে। 
# আপনি যদি গিটহাবে সরাসরি Credentials দিয়ে থাকেন, তবে নিচে সেটি ইনপুট দিন।
# আপাতত আমি সহজ রাখার জন্য সরাসরি ডাটা এন্ট্রির ফরম্যাট দিচ্ছি।

st.title("🐔 খামার ডায়েরি (Farm Manager)")
st.subheader("প্রতিদিনের হিসাব এখানে জমা দিন")

# ডাটা ইনপুট ফর্ম
with st.form("farm_form"):
    date = st.date_input("তারিখ", datetime.now())
    eggs = st.number_input("ডিম সংখ্যা (Eggs)", min_value=0, step=1)
    feed = st.number_input("খাবার খরচ/পরিমাণ (Feed)", min_value=0.0)
    medicine = st.text_input("ওষুধের নাম/খরচ (Medicine)")
    
    submitted = st.form_submit_button("জমা দিন (Submit)")

if submitted:
    st.success(f"সফলভাবে জমা হয়েছে! তারিখ: {date}, ডিম: {eggs}")
    # এখানে শিটে ডাটা পাঠানোর কোডটি রান হবে
